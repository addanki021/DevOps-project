pipeline {
   agent any
stages {
    stage('Build') {
        steps {
             echo 'Building'
     stage('deploy') {
         steps {
              echo 'Deploying'
                 }
               }
            }
        }
   }

